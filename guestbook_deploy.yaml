apiVersion: apps/v1
kind: Deployment
metadata:
  name: guestbook
spec:
  replicas: 2
  selector:
    matchLabels:
      app: guestbook
  template:
    metadata:
      name: guestbook
      labels:
        app: guestbook
    spec:
      containers:
      - image: yu3papa/cicd_guestbook:20241222110032_8
        name: sb-guestbook

---
apiVersion: v1
kind: Service
metadata:
  name: guestbook-http
spec:
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: guestbook
